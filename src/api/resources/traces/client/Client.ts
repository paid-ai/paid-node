// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import * as errors from "../../../../errors/index.js";
import * as Paid from "../../../index.js";

export declare namespace Traces {
    export interface Options extends BaseClientOptions {}

    export interface RequestOptions extends BaseRequestOptions {}
}

export class Traces {
    protected readonly _options: Traces.Options;

    constructor(_options: Traces.Options = {}) {
        this._options = _options;
    }

    /**
     * @param {Paid.GetTracesRequest} request
     * @param {Traces.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Paid.BadRequestError}
     * @throws {@link Paid.ForbiddenError}
     *
     * @example
     *     await client.traces.getTraces({
     *         limit: 1,
     *         offset: 1,
     *         startTime: "2024-01-15T09:30:00Z",
     *         endTime: "2024-01-15T09:30:00Z",
     *         externalCustomerId: "externalCustomerId",
     *         externalProductId: "externalProductId",
     *         externalAgentId: "externalAgentId",
     *         metadata: "metadata"
     *     })
     */
    public getTraces(
        request: Paid.GetTracesRequest = {},
        requestOptions?: Traces.RequestOptions,
    ): core.HttpResponsePromise<Paid.TracesResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getTraces(request, requestOptions));
    }

    private async __getTraces(
        request: Paid.GetTracesRequest = {},
        requestOptions?: Traces.RequestOptions,
    ): Promise<core.WithRawResponse<Paid.TracesResponse>> {
        const { limit, offset, startTime, endTime, externalCustomerId, externalProductId, externalAgentId, metadata } =
            request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (limit != null) {
            _queryParams.limit = limit.toString();
        }

        if (offset != null) {
            _queryParams.offset = offset.toString();
        }

        if (startTime != null) {
            _queryParams.startTime = startTime;
        }

        if (endTime != null) {
            _queryParams.endTime = endTime;
        }

        if (externalCustomerId != null) {
            _queryParams.externalCustomerId = externalCustomerId;
        }

        if (externalProductId != null) {
            _queryParams.externalProductId = externalProductId;
        }

        if (externalAgentId != null) {
            _queryParams.externalAgentId = externalAgentId;
        }

        if (metadata != null) {
            _queryParams.metadata = metadata;
        }

        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PaidEnvironment.Production,
                "traces",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Paid.TracesResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Paid.BadRequestError(_response.error.body as Paid.Error_, _response.rawResponse);
                case 403:
                    throw new Paid.ForbiddenError(_response.error.body as Paid.Error_, _response.rawResponse);
                default:
                    throw new errors.PaidError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PaidError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PaidTimeoutError("Timeout exceeded when calling GET /traces.");
            case "unknown":
                throw new errors.PaidError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getAuthorizationHeader(): Promise<string | undefined> {
        const bearer = await core.Supplier.get(this._options.token);
        if (bearer != null) {
            return `Bearer ${bearer}`;
        }

        return undefined;
    }
}
