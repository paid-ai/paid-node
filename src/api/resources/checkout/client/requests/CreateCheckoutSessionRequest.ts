// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         product_id: "product_id",
 *         success_url: "success_url",
 *         cancel_url: "cancel_url"
 *     }
 */
export interface CreateCheckoutSessionRequest {
    /** ID of the product to create a checkout session for */
    product_id: string;
    /** Existing customer ID (either this or external_customer_id required) */
    customer_id?: string | null;
    /** External customer ID from your system (either this or customer_id required) */
    external_customer_id?: string | null;
    /** URL to redirect to after successful checkout */
    success_url: string;
    /** URL to redirect to if checkout is canceled */
    cancel_url: string;
    /** Optional metadata to attach to the session */
    metadata?: Record<string, unknown> | null;
}
