// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import * as errors from "../../../../errors/index.js";
import * as Paid from "../../../index.js";

export declare namespace Checkout {
    export interface Options extends BaseClientOptions {}

    export interface RequestOptions extends BaseRequestOptions {}
}

export class Checkout {
    protected readonly _options: Checkout.Options;

    constructor(_options: Checkout.Options) {
        this._options = _options;
    }

    /**
     * Creates a new checkout session for a product
     *
     * @param {Paid.CreateCheckoutSessionRequest} request
     * @param {Checkout.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Paid.BadRequestError}
     * @throws {@link Paid.NotFoundError}
     * @throws {@link Paid.InternalServerError}
     *
     * @example
     *     await client.checkout.createCheckoutSession({
     *         product_id: "product_id",
     *         success_url: "success_url",
     *         cancel_url: "cancel_url"
     *     })
     */
    public createCheckoutSession(
        request: Paid.CreateCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): core.HttpResponsePromise<Paid.CheckoutSession> {
        return core.HttpResponsePromise.fromPromise(this.__createCheckoutSession(request, requestOptions));
    }

    private async __createCheckoutSession(
        request: Paid.CreateCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): Promise<core.WithRawResponse<Paid.CheckoutSession>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PaidEnvironment.Default,
                "checkout/sessions/",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Paid.CheckoutSession, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Paid.BadRequestError(_response.error.body as Paid.ErrorResponse, _response.rawResponse);
                case 404:
                    throw new Paid.NotFoundError(_response.error.body as Paid.ErrorResponse, _response.rawResponse);
                case 500:
                    throw new Paid.InternalServerError(
                        _response.error.body as Paid.ErrorResponse,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PaidError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PaidError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PaidTimeoutError("Timeout exceeded when calling POST /checkout/sessions/.");
            case "unknown":
                throw new errors.PaidError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Retrieves details of a checkout session
     *
     * @param {Paid.GetCheckoutSessionRequest} request
     * @param {Checkout.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Paid.NotFoundError}
     * @throws {@link Paid.InternalServerError}
     *
     * @example
     *     await client.checkout.getCheckoutSession({
     *         sessionId: "sessionId"
     *     })
     */
    public getCheckoutSession(
        request: Paid.GetCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): core.HttpResponsePromise<Paid.CheckoutSessionDetails> {
        return core.HttpResponsePromise.fromPromise(this.__getCheckoutSession(request, requestOptions));
    }

    private async __getCheckoutSession(
        request: Paid.GetCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): Promise<core.WithRawResponse<Paid.CheckoutSessionDetails>> {
        const { sessionId } = request;
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PaidEnvironment.Default,
                `checkout/sessions/${core.url.encodePathParam(sessionId)}`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Paid.CheckoutSessionDetails, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 404:
                    throw new Paid.NotFoundError(_response.error.body as Paid.ErrorResponse, _response.rawResponse);
                case 500:
                    throw new Paid.InternalServerError(
                        _response.error.body as Paid.ErrorResponse,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PaidError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PaidError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PaidTimeoutError("Timeout exceeded when calling GET /checkout/sessions/{sessionId}.");
            case "unknown":
                throw new errors.PaidError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Cancels a pending checkout session
     *
     * @param {Paid.CancelCheckoutSessionRequest} request
     * @param {Checkout.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Paid.BadRequestError}
     * @throws {@link Paid.NotFoundError}
     * @throws {@link Paid.InternalServerError}
     *
     * @example
     *     await client.checkout.cancelCheckoutSession({
     *         sessionId: "sessionId"
     *     })
     */
    public cancelCheckoutSession(
        request: Paid.CancelCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): core.HttpResponsePromise<Paid.CancelCheckoutSessionResponse> {
        return core.HttpResponsePromise.fromPromise(this.__cancelCheckoutSession(request, requestOptions));
    }

    private async __cancelCheckoutSession(
        request: Paid.CancelCheckoutSessionRequest,
        requestOptions?: Checkout.RequestOptions,
    ): Promise<core.WithRawResponse<Paid.CancelCheckoutSessionResponse>> {
        const { sessionId } = request;
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PaidEnvironment.Default,
                `checkout/sessions/${core.url.encodePathParam(sessionId)}/cancel`,
            ),
            method: "POST",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Paid.CancelCheckoutSessionResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Paid.BadRequestError(_response.error.body as Paid.ErrorResponse, _response.rawResponse);
                case 404:
                    throw new Paid.NotFoundError(_response.error.body as Paid.ErrorResponse, _response.rawResponse);
                case 500:
                    throw new Paid.InternalServerError(
                        _response.error.body as Paid.ErrorResponse,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PaidError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PaidError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PaidTimeoutError(
                    "Timeout exceeded when calling POST /checkout/sessions/{sessionId}/cancel.",
                );
            case "unknown":
                throw new errors.PaidError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getAuthorizationHeader(): Promise<string> {
        return `Bearer ${await core.Supplier.get(this._options.token)}`;
    }
}
